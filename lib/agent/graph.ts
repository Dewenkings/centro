/**
 * Centro Agent — LangGraph.js StateGraph
 * Day 2 实现：编排 geocode → computeCenter → searchPOI → planRoutes → rankResults
 *
 * 当前为骨架文件，Day 2 填充 Node 实现和 Edge 连接
 */

import { StateGraph } from "@langchain/langgraph";
import { GatherState } from "./state";

// TODO: Day 2 实现各 Node
// const parseInputNode = async (state: GatherState) => { ... };
// const geocodeNode = async (state: GatherState) => { ... };
// ...

// TODO: Day 2 构建 Graph
// export const graph = new StateGraph<GatherState>({ channels: { ... } })
//   .addNode("parseInput", parseInputNode)
//   .addNode("geocode", geocodeNode)
//   ...
//   .addEdge("__start__", "parseInput")
//   .compile();

export {};
